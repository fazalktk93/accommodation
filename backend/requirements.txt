# backend/requirements.txt  -- clean v1 stack

fastapi==0.99.1
uvicorn[standard]==0.30.1

SQLAlchemy==2.0.30
alembic==1.13.2

# Pydantic v1 + dotenv support
pydantic[dotenv]>=1.10,<2
# (you can keep python-dotenv if other scripts import it directly; not required if using only BaseSettings)
python-dotenv>=1.0,<2.0

# Auth / utils
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.9.0
itsdangerous==2.2.0
email-validator==1.3.1
python-dateutil==2.9.0.post0

# Admin UI
sqladmin==0.16.0

# prod server (optional if you use gunicorn)
gunicorn==22.0.0
